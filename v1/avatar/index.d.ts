export interface AvatarProps {
    altText: string;
    title: string;
    imageUrl: string;
}
export declare function Avatar({ altText, title, imageUrl }: AvatarProps): import("react/jsx-runtime").JSX.Element;
